# Prima del vulnerability scan con Docker Scout
# FROM nginx:latest
FROM nginx:alpine

# Copia della configurazione personalizzata
COPY nginx.conf /etc/nginx/nginx.conf

# Copia della cartella dei certificati
COPY certs /etc/nginx/certs